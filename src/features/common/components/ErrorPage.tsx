import React from 'react';

export default function ErrorPage(): JSX.Element{
  return (
        <div>
            Something went wrong when loading data. Try refreshing the page. 
        </div>
  );
}